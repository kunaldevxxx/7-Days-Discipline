// make an array of objects representingv item ,filter the item on a specific condition (eg. item with certain property value) and
// transform the filtered data into new format
//given an array of objects with a numerical property ,implement a function to sort the objects based on property . additionaly 
//add a ranking to each object based on its position in the sorted array
